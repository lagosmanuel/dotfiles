local g = vim.g

g.tmux_navigator_no_mappings = 1 -- Define custom tmux navigator maps.
--g.tmux_navigator_save_on_switch = 2 -- Write all buffers before navigating from Vim to tmux pane.
g.tmux_navigator_disable_when_zoomed = 1 -- Disable tmux navigator when zooming the Vim pane.
g.tmux_navigator_preserve_zoom = 1 -- If the tmux window is zoomed, keep it zoomed when moving from Vim to another pane.

